# 安全策略

## 支持范围

安全修复优先面向最新版本。旧版本可能不会单独回补；用户应升级到最新 Release。

## 报告漏洞

请优先使用 GitHub 仓库的 **Private vulnerability reporting**。不要在公开 Issue 中提交登录凭据、Cookie、Token、邮箱、账户响应、会话内容、项目代码或可直接利用的漏洞细节。

报告建议包含：

- 受影响版本和 macOS 版本；
- 可复现的最小步骤；
- 实际结果与预期结果；
- 影响范围及是否涉及数据泄露；
- 已脱敏的日志、截图或最小样例。

维护者确认问题前请避免公开披露。普通崩溃、界面问题和不包含敏感信息的数据源兼容问题可使用公开 Issue。

## 安全边界

- 应用不是密码管理器，不提供密码输入框；
- WebKit 登录态由系统 WebKit 存储；
- 本机 Codex 凭据由 Codex 自身管理，本应用不直接读取 `auth.json`；
- 调试信息必须经过 `SensitiveDataRedactor`；
- 未签名或 ad-hoc 签名构建仅用于开发测试。
