# 版本记录

本项目采用 `主版本.次版本.修订版本` 格式。日期使用 Asia/Shanghai 时区。

## 1.8.8 — 2026-07-17

### 修复

- app-server 额度读取改为先完成 `account/read(refreshToken: true)`，再调用 `account/rateLimits/read`，消除冷会话中的凭据刷新竞态；
- 对服务暂时不可用、overloaded、timeout、连接重置等瞬时故障增加一次 0.7–1.3 秒抖动重试；
- 本机额度冷启动总容限调整为 45 秒，继续保留成功后的 45 秒短缓存；
- 新增瞬时故障重试策略测试，总计 68 项 XCTest。

### 分发

- ad-hoc 构建产物强制带 `local-test` 文件名，并在构建输出中明确警告不能跨 Mac 分享；
- 正式 DMG 继续强制要求 Developer ID Application 与 Apple 公证，避免把本机测试包误发给其他用户。

## 1.8.7 — 2026-07-16

### 修复

- 修复本机 Codex 授权被清除后，普通刷新只显示“无法读取”却不给恢复入口的问题；
- 菜单面板和完整面板的刷新按钮会在缺少授权时直接显示隐私说明并请求重新授权；
- 未授权时快捷操作明确显示“授权本机 Codex”，不再使用含义模糊的“复用本机 Codex”；
- 刷新失败信息会区分授权缺失，并说明今日 Token 是保留的上次结果；
- “清除全部本地数据”增加破坏性二次确认，并明确告知会同时撤销两个本机数据源授权。
- 修复 Xcode 16.4 / Swift 6.0 对通知设置并发边界与套餐倍数类型推断的兼容性构建错误。

## 1.8.6 — 2026-07-16

### 修复

- 撤销 1.8.4 将本机额度超时激进缩短至 7 秒的回归，冷启动容限调整为 35 秒；
- 实测官方 `account/rateLimits/read` 冷请求可能耗时 29.554 秒，而随后热请求约为 5.053 秒；
- 缓存或估算成功返回时不再清除之前真实数据源的失败原因，便于准确诊断；
- 保留 45 秒真实快照短缓存，使首次成功后的连续刷新立即完成。

## 1.8.5 — 2026-07-16

### 修复

- 修正缓存回退被误判为刷新成功的问题：现在保留数据的同时显示真实失败原因；
- 刷新期间无论旧快照来自实时、缓存还是估算，状态栏都立即显示“正在读取 Codex 数据”；
- 菜单面板与完整面板的按钮在执行期间明确显示“刷新中…”，避免点击后看似没有反应；
- 不再由外层网络状态提前拦截本机 Codex 数据源，实际读取与回退统一交给 Repository 诊断。

### 调研

- 使用 OpenAI Codex CLI 0.144.5 官方生成的实验性 app-server JSON Schema 核对 `account/read`、`account/rateLimits/read`、`account/usage/read` 与 `account/rateLimits/updated`；
- 按 Schema 要求保留完整额度快照，并将滚动通知视为稀疏更新，不用缺失字段覆盖既有值。

## 1.8.4 — 2026-07-16

### 性能

- 额度查询与本机实时 Token 扫描改为并发执行，避免串行等待；
- 最近一次本机真实额度在 45 秒内复用，再次点击刷新可立即返回，并保留原始来源与更新时间；
- Analytics 改为额度显示后的独立后台补充，不再占用刷新按钮任务或拦截下一次刷新；
- 本机额度源超时由通用 15 秒收紧为 7 秒，失败时更快进入缓存或估算降级。

### 修复

- 额度先保存和通知，Analytics 返回后更新同一条历史记录，避免异步补充产生重复快照。

## 1.8.3 — 2026-07-16

### 修复

- 将稳定按压反馈扩展至主按钮、玻璃按钮、紧凑按钮和菜单工具按钮，并彻底移除这些样式的缩放变换层；
- 移除 Dashboard 与设置页选择变化时作用于整块内容的弹簧和位移动画；
- 移除背景渐变层的额外缩放，避免父级动画继承造成整体放大的视觉效果；
- 保留按钮自身的填充色和透明度变化，使点击状态仍然清晰可见。

## 1.8.2 — 2026-07-16

### 修复

- 移除菜单面板按钮按压时的缩放和弹簧动画，解决 `MenuBarExtra(.window)` 宿主可能将局部缩放表现为整块面板放大的问题；
- 为紧凑操作和工具按钮增加仅作用于按钮自身的颜色、填充和透明度反馈；
- 新增回归测试，确保按下和未按下状态不改变布局缩放。

### 文档

- 补齐功能、数据来源、读取范围、准确性、架构、测试、打包、常见问题和免责声明；
- 新增贡献指南、安全策略及 GitHub Issue/PR 模板；
- 同步构建报告至 1.8.2（Build 25）和 63 项测试。

## 1.8.1 — 2026-07-16

- 完善本机 Codex app-server 额度、Token 日汇总与账户摘要读取；
- 增加本机实时今日 Token、独立 Analytics 合并及紧凑面板里程碑；
- 完善 Universal 2 DMG 打包、登录项、隐私诊断和脱敏测试。
