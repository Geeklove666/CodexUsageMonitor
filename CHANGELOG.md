# 版本记录

本项目采用 `主版本.次版本.修订版本` 格式。日期使用 Asia/Shanghai 时区。

## 2.1.10 — 2026-07-28

### 每日用量口径修复

- 菜单弹窗与最近 7 天总结统一为真实 Token 优先，包括有效的 `0 Token` 记录；
- 仅在当天完全没有 Token 记录时，才根据 Credits 余额下降量显示明确标注的估算值；
- Credits 增加、官方恢复或切换账户时只重建估算基线，不产生负消耗，也不抵扣当天已有消耗；
- 增加逐日 Token 记录存在性，避免数据合并把“无记录”错误制造成 `0 Token`；
- 最近 7 天不再混合累加 Token 与 Credits，不同单位按日分别标识；
- 新增 Token 优先、零值、余额下降、余额增加、缓存样本和跨账户等回归测试；
- 版本提升为 2.1.10（Build 81）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.9 — 2026-07-24

### 套餐 Credits 官方口径

- 移除自定义“1 亿 Token = 1 个小目标”换算；
- 本机日志按 OpenAI 公布的模型费率分别计算非缓存输入、缓存输入和输出 Token 对应的 ChatGPT Credits；
- 支持 GPT-5.6 Sol、Terra、Luna、GPT-5.5、GPT-5.4 与 GPT-5.4 mini，并应用 Fast 模式的官方倍率；
- 套餐剩余百分比与重置窗口继续直接读取 OpenAI 服务端值，不用本机统计反推；
- 菜单弹窗与最近 7 天总结改为优先展示套餐 Credits，同时保留有效 Token 作为辅助信息；
- 未知或预览模型不推测计价，避免把不完整数据伪装成精确结果；
- 版本提升为 2.1.9（Build 80）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.8 — 2026-07-24

### 统计口径

- 每日主数值改为“有效 Token”，按 `total_tokens - cached_input_tokens` 计算，避免把重复读取的缓存上下文当成新增消耗；
- 本机扫描缓存同时保存总处理量与缓存输入增量，仍不读取会话正文；
- 菜单弹窗、每日表格、授权说明和隐私文档统一标明“不含缓存”口径；
- 增加缓存输入扣除的回归测试。

### 分发

- 版本提升为 2.1.8（Build 79）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.7 — 2026-07-24

### 统计修复

- 修复本机实时 Token 与账号历史用量使用不同零点时间戳时，同一自然日被识别为两条记录并重复相加的问题；
- 每日 Token 合并统一按本机自然日去重，实时数据优先覆盖延迟历史数据；
- 增加不同时区时间戳落在同一自然日的回归测试。

### 分发

- 版本提升为 2.1.7（Build 78）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.6 — 2026-07-23

### 界面与交互

- 完整面板默认打开“总结”，并将“概览”改名为“额度详情”，减少入口语义重复；
- 重新梳理总结页额度卡的百分比、重置时间与状态层级，每日 Token 表改用中性蓝色并突出当天记录；
- 使用历史增加末端数据标记和鼠标悬停明细；刷新中状态不再同时占用多个页面位置；
- 数据来源改用紧凑栅格对齐，设置内容限制阅读宽度，菜单弹窗合并重复的额度摘要与进度区。
- 打开完整面板或切换页面时回到内容顶部，避免系统恢复旧滚动位置后截断主额度卡。

### 稳定性

- 本机 Token 局部更新不再覆盖额度刷新诊断原因，避免诊断页把两个独立刷新链路混为一谈；
- 100 项 XCTest 全部通过。

### 分发

- 版本提升为 2.1.6（Build 77）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.5 — 2026-07-23

### 界面

- 恢复到今日 UI 调整前的 2.1.2 视觉基线：360pt 菜单弹窗、原额度卡与 Token 卡、总结/概览/告警六项导航；
- 保留后续新增的 Provider、Claude Code 本机用量、导出和刷新链路，不回退功能修复。

### 性能与稳定性

- 未授权本机 Token 或 Claude Code 用量时不再启动对应 FSEvents 监听；仅监听用户实际启用的数据目录；
- 多个附加 AI Provider 改为并发读取，避免未来扩展服务时串行拖慢刷新；
- Claude Code 日志解析复用 ISO 8601 日期解析器，避免逐行重复创建格式化对象。

### 分发

- 版本提升为 2.1.5（Build 76）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.4 — 2026-07-23

### 界面

- 额度主卡、今日 Token 卡以及完整面板核心数据区改用原生 Liquid Glass，自 macOS 26 起使用 `glassEffect`，macOS 15–25 使用系统 Material 回退；
- 菜单弹窗宽度由 348pt 收窄为 336pt，主卡环形进度增大并增加纵向留白，改善卡片矮胖比例；
- 核心摘要、快速指标、每日 Token、其他服务与使用趋势使用轻量 Glass，设置和密集诊断表仍保持稳定实色；
- 降低透明度模式继续使用高对比稳定背景，不依赖透明材质。

### 分发

- 版本提升为 2.1.4（Build 75）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.3 — 2026-07-23

### 界面

- 统一菜单弹窗与完整面板的 Apple 原生视觉规范，数据卡片改为中性实色，Liquid Glass 仅保留在导航、按钮和弹窗等交互层；
- 菜单弹窗移除重复的主额度进度区，首屏按剩余额度、重置时间、今日 Token 排列，并将辅助操作收敛为紧凑工具栏；
- 完整面板将“总结/概览”合并为“概览”，将“告警/设置”合并为“设置”，侧边栏由六项精简为四项；
- 主额度卡改为大号状态环、重置时间和状态色进度组合，趋势颜色随当前剩余比例变化；
- 统一使用绿、蓝、黄、橙、红五档额度状态色，并保留数字过渡、进度变化和刷新反馈。

### 质量

- 删除菜单内不再使用的重复进度组件和旧按钮样式；
- 100 项 XCTest 全部通过；
- 版本提升为 2.1.3（Build 74）；仅生成本机确认 DMG，不同步 GitHub。

## 2.1.2 — 2026-07-22

### 功能

- Provider 描述增加能力与认证方式声明，为后续服务接入提供可测试契约；
- 新增用户授权的本机 Claude Code 最近 7 天 Token 用量，明确不等同于套餐额度；
- 新增版本化用量 JSON、历史 CSV 和脱敏诊断 JSON 导出；
- 使用 macOS FSEvents 监听 Codex/Claude 本机会话变化，仅触发局部 Token 更新。

### 隐私与质量

- Claude 解析只读取结构化 usage 数值、消息 ID 和时间戳，消息 ID 仅用于当次去重；
- 导出模型排除账号身份，诊断字段再次经过敏感信息脱敏；
- 新增 Provider 能力、Claude 去重解析和导出隐私回归测试。

### 分发

- 版本提升为 2.1.2（Build 73）；仅生成本机测试 DMG，不同步 GitHub。

## 2.0.37 — 2026-07-20

### 功能

- 每日 Token 用量表增加重置次数，合并本机检测到的手动重置消耗与官方周期重置；
- 仅在本机额度历史覆盖对应日期时显示次数，缺少历史时明确显示为不可用。

### 体验

- 今日 Token 小目标图标改用克制的系统中性色；
- 精简刷新中的重复状态反馈，并完善总结页统计文案与辅助功能描述。

### 分发

- 版本提升为 2.0.37（Build 69），同步 GitHub。

## 2.0.36 — 2026-07-20

### 功能

- 完整面板新增总结页，集中展示当前额度、消耗速度和最近 7 天每日 Token；
- 最近 7 天 Token 表提供合计、有记录日均、峰值和小目标换算；
- 本机 Token 统计只读取结构化 `token_count` 事件和时间戳，不读取会话正文。

### 体验

- 今日 Token 小目标图标改为中性系统样式；
- 刷新状态减少重复动画，只保留必要反馈；
- 每日 Token 表修正 VoiceOver 重复朗读，并明确有记录天数与日均口径。

### 分发

- 版本提升为 2.0.36（Build 68），同步 GitHub。

## 2.0.17 — 2026-07-17

### 改进

- 参考 CodexBar 的 Codex 数据读取策略，增强本机 Codex app-server 原始额度解析；
- 支持解析 `rateLimitsByLimitId` / `rate_limits_by_limit_id`，当主额度结构不完整时可从子额度补充 Credits 与个人消费限制信息；
- 快照新增账号身份信息，历史与缓存恢复会保留 normalized email、account id 与套餐标识；
- 本机 Codex 返回已登录账号或 Credits 但缺少额度窗口时，不再直接判定为读取失败，而是返回部分快照并明确显示不可用字段；
- 手动刷新继续强制绕过本机短缓存，避免点击刷新仍读旧额度。

### 分发

- 版本提升为 2.0.17（Build 49），同步 GitHub。

## 2.0.16 — 2026-07-17

### 修复

- 完整面板默认改为使用真实 `monitor.snapshot`，不再打开早期 Demo 界面壳；
- 移除完整面板顶部 `Demo 状态` 选择器、侧边栏 Demo 说明和主卡片 Demo 标记；
- Overview、数据来源、额度表和告警区域改为展示当前真实快照的来源、额度、Credits 与重置次数；
- 保留 loading、cached、estimated、offline、unavailable、exhausted 等状态表达，但不再作为 Demo 数据显示。

### 分发

- 版本提升为 2.0.16（Build 48），同步 GitHub。

## 2.0.15 — 2026-07-17

### 界面

- 菜单栏顶部 `Codex` 状态文字改回系统默认文字颜色；
- 菜单栏顶部状态文字使用系统菜单栏常规字体，不再按额度状态显示绿色、黄色或红色文字。

### 清理

- 清理本地构建缓存和 macOS `.DS_Store` 临时文件；
- 移除菜单栏标题颜色相关的无用辅助代码。

### 分发

- 版本提升为 2.0.15（Build 47），同步 GitHub。

## 2.0.14 — 2026-07-17

### 修复

- 兼容本机 Codex `rateLimitResetCredits.credits: null` 的真实返回结构，避免刷新后暴露系统英文解码错误；
- 本机 Codex 解析失败统一转换为中文错误信息，菜单栏不再直接显示底层 `DecodingError`；
- 已启用本机 Codex 后，菜单按钮改为“刷新本机 Codex”，点击只刷新额度，不再重复执行 `codex login` 并打开网页登录；
- 本机 Codex 额度读取超时单独放宽至 45 秒，降低冷启动或后端慢响应时误判失败的概率。

### 分发

- 版本提升为 2.0.14（Build 46），同步 GitHub。

## 2.0.13 — 2026-07-17

### 界面

- 菜单栏弹窗从 SwiftUI `MenuBarExtra(.window)` 切换为自建 `NSStatusItem` 和透明 `NSPanel`；
- 弹窗宿主窗口设置为透明、非 opaque、无系统阴影，内容由单一圆角主面板绘制；
- 继续保持主面板内部卡片、按钮、排版和功能不变。

### 数据显示

- Credits 剩余额度统一保留小数点后两位显示；
- README 改为功能说明，不再展示历史修复清单。

### 分发

- 版本提升为 2.0.13（Build 45），同步 GitHub。

## 2.0.12 — 本地开发中，尚未发布

### 功能

- 参考 CodexBar 的刷新策略，新增“智能”自动刷新模式；
- 菜单栏弹窗打开时只在数据不可用、失败、缓存/估算或超过 2 分钟未更新时触发刷新；
- 完整面板“数据来源”页新增“真实刷新诊断”，展示最近一次真实刷新链路、数据源状态、耗时、成功/失败原因与触发来源。

### 优化

- 智能刷新会根据最近菜单打开时间、低电量模式和系统热状态在 1/5/10 分钟之间调节；
- 数据源刷新诊断记录每个来源的 availability、耗时和脱敏错误，便于定位“刷新没反应”或“响应慢”；
- 继续保留刷新失败 backoff，避免异常状态下持续高频请求。

### 分发

- 版本提升为 2.0.12（Build 44）。本版本仅本地构建，未同步 GitHub。

## 2.0.11 — 本地开发中，准备同步 GitHub

### 功能

- 完整面板“设置”页新增自动刷新频率选择；
- 自动刷新频率支持 1 分钟、5 分钟、10 分钟；
- 设置页“常规”也同步提供同一刷新频率入口，修改后会重启后台刷新循环，下一轮按新频率执行。

### 优化

- 后台刷新调度从旧的 active / idle 隐藏参数收敛为单一可见配置，减少不可预期刷新；
- 菜单栏弹窗宿主窗口配置增加窗口级缓存，避免 SwiftUI 多次更新时重复遍历 NSView 层级；
- 保留刷新失败 backoff，连续失败时不会因为用户选择 1 分钟而持续高频请求。

### 分发

- 版本提升为 2.0.11（Build 43）。本版本经用户确认后同步 GitHub。

## 2.0.10 — 本地开发中，尚未发布

### 修复

- 修复菜单栏弹窗四周残留的外层矩形面板；
- 将 MenuBarExtra 宿主 NSWindow 配置为透明、非 opaque、无系统窗口阴影；
- 清理 NSHostingView / contentView 背景，移除窗口级背景可见性；
- 菜单根 View 只绘制一个与实际内容尺寸一致的圆角主面板，圆角外透明。

### 分发

- 版本提升为 2.0.10（Build 42）。本版本仅本地构建，未同步 GitHub。

## 2.0.9 — 本地开发中，尚未发布

### 界面

- 移除菜单栏展开面板背后的自定义半透明 Material 遮罩；
- 面板背景回到普通应用背景，不再额外绘制整块灰色玻璃底。

### 分发

- 版本提升为 2.0.9（Build 41）。本版本仅本地构建，未同步 GitHub。

## 2.0.8 — 本地开发中，尚未发布

### 界面

- 取消 2.0.5 引入的 MenuBarExtra 宿主窗口透明/连续圆角配置；
- 菜单栏展开面板回到 2.0.4 的内部面板圆角与系统材质样式。

### 分发

- 版本提升为 2.0.8（Build 40）。本版本仅本地构建，未同步 GitHub。

## 2.0.7 — 本地开发中，尚未发布

### 界面

- 取消 2.0.6 引入的 Apple 电池充电绿填充色；
- 设置页数据源区域的主操作改为中性 Liquid Glass，不再使用大面积绿色或蓝色 tint。

### 分发

- 版本提升为 2.0.7（Build 39）。本版本仅本地构建，未同步 GitHub。

## 2.0.6 — 本地开发中，尚未发布

### 界面

- 设置页数据源区域的填充型主操作改为类似 Apple 电池充电状态的绿色，不再使用大面积蓝色；
- 本机 Codex、OpenAI 回退登录和实时今日 Token 的授权勾选与成功状态统一使用充电绿语义色。

### 分发

- 版本提升为 2.0.6（Build 38）。本版本仅本地构建，未同步 GitHub。

## 2.0.5 — 本地开发中，尚未发布

### 修复

- 菜单栏展开面板不再只画内部假圆角；现在会把 MenuBarExtra 宿主窗口配置为透明非 opaque，并对宿主内容层应用连续圆角裁切，避免外层矩形边缘露出。

### 分发

- 版本提升为 2.0.5（Build 37）。本版本仅本地构建，未同步 GitHub。

## 2.0.4 — 本地开发中，尚未发布

### 修复

- 刷新不再主动弹出 OpenAI 网页登录窗口；需要网页登录时由用户手动点击“OpenAI 登录”；
- 菜单展开面板改为更接近 Apple popover 的大连续圆角、系统材质与柔和描边；
- 裸 `pro` / `Pro` 套餐按美区 Pro 20x 处理，只有明确 `pro_5x` / `pro 5x` / `prolite` 才显示为 Pro 5x。

### 分发

- 版本提升为 2.0.4（Build 36）。

## 2.0.3 — 本地开发中，尚未发布

### 界面

- 移除菜单面板“完整面板”按钮的大面积蓝色 tint，改为与“刷新”一致的中性 Liquid Glass 控件。

### 分发

- 版本提升为 2.0.3（Build 35）。

## 2.0.2 — 本地开发中，尚未发布

### 界面

- 移除菜单面板中的“更新今日 Token”独立按钮，统一由“刷新”同步刷新额度和今日 Token；
- 底部功能区改为更低饱和的系统材质与细描边风格，减少黑白强对比；
- 完整面板导航、状态、说明、价格基准与 Demo 文案中文化。

### 分发

- 版本提升为 2.0.2（Build 34）。

## 2.0.1 — 本地开发中，尚未发布

### 数据源

- 重新加入“本机 Codex 登录”额度源：用户授权后优先调用 OpenAI 签名的本机 `codex` 命令读取额度；
- 新增 `codex login status` 预检和设置页登录状态显示；
- 菜单栏新增“启用本机 Codex / Codex 登录”入口，官方 OpenAI 页面继续作为回退来源；
- 新增本机 app-server rate limits 解析回归测试。

### 分发

- 版本提升为 2.0.1（Build 33）。

## 2.0.0 — 本地开发中，尚未发布

### 架构

- 移除 OpenAI 官方仍标记为 Experimental 的 Codex `app-server` 登录复用和额度读取；
- 实时额度统一使用应用内隔离的 OpenAI 官方页面会话，缓存与本地估算继续作为降级来源；
- 本机 `token_count` 统计保留为独立授权，不再与登录复用绑定；
- 部署目标改为 macOS 15，发行架构收敛为 Apple Silicon arm64。

### 设计

- 按 Apple Liquid Glass 层级重做背景、导航、按钮、菜单工具区与登录页；
- macOS 26/27 使用原生 `glassEffect`，macOS 15 使用系统 Material 回退；
- 内容卡片保持标准材质，避免玻璃效果淹没数据层级和可读性。

### 分发

- DMG 文件名改为 `apple-silicon`，构建脚本强制验证单一 arm64 架构和 macOS 15 最低版本；
- 本版本仅保留在本地，未经用户确认不得提交、推送或发布到 GitHub。

## 1.8.8 — 2026-07-17

### 修复

- app-server 额度读取改为先完成 `account/read(refreshToken: true)`，再调用 `account/rateLimits/read`，消除冷会话中的凭据刷新竞态；
- 对服务暂时不可用、overloaded、timeout、连接重置等瞬时故障增加一次 0.7–1.3 秒抖动重试；
- 本机额度冷启动总容限调整为 45 秒，继续保留成功后的 45 秒短缓存；
- 新增瞬时故障重试策略测试，总计 68 项 XCTest。

### 分发

- ad-hoc 构建产物强制带 `local-test` 文件名，并在构建输出中明确警告不能跨 Mac 分享；
- 正式 DMG 继续强制要求 Developer ID Application 与 Apple 公证，避免把本机测试包误发给其他用户。

## 1.8.7 — 2026-07-16

### 修复

- 修复本机 Codex 授权被清除后，普通刷新只显示“无法读取”却不给恢复入口的问题；
- 菜单面板和完整面板的刷新按钮会在缺少授权时直接显示隐私说明并请求重新授权；
- 未授权时快捷操作明确显示“授权本机 Codex”，不再使用含义模糊的“复用本机 Codex”；
- 刷新失败信息会区分授权缺失，并说明今日 Token 是保留的上次结果；
- “清除全部本地数据”增加破坏性二次确认，并明确告知会同时撤销两个本机数据源授权。
- 修复 Xcode 16.4 / Swift 6.0 对通知设置并发边界与套餐倍数类型推断的兼容性构建错误。

## 1.8.6 — 2026-07-16

### 修复

- 撤销 1.8.4 将本机额度超时激进缩短至 7 秒的回归，冷启动容限调整为 35 秒；
- 实测官方 `account/rateLimits/read` 冷请求可能耗时 29.554 秒，而随后热请求约为 5.053 秒；
- 缓存或估算成功返回时不再清除之前真实数据源的失败原因，便于准确诊断；
- 保留 45 秒真实快照短缓存，使首次成功后的连续刷新立即完成。

## 1.8.5 — 2026-07-16

### 修复

- 修正缓存回退被误判为刷新成功的问题：现在保留数据的同时显示真实失败原因；
- 刷新期间无论旧快照来自实时、缓存还是估算，状态栏都立即显示“正在读取 Codex 数据”；
- 菜单面板与完整面板的按钮在执行期间明确显示“刷新中…”，避免点击后看似没有反应；
- 不再由外层网络状态提前拦截本机 Codex 数据源，实际读取与回退统一交给 Repository 诊断。

### 调研

- 使用 OpenAI Codex CLI 0.144.5 官方生成的实验性 app-server JSON Schema 核对 `account/read`、`account/rateLimits/read`、`account/usage/read` 与 `account/rateLimits/updated`；
- 按 Schema 要求保留完整额度快照，并将滚动通知视为稀疏更新，不用缺失字段覆盖既有值。

## 1.8.4 — 2026-07-16

### 性能

- 额度查询与本机实时 Token 扫描改为并发执行，避免串行等待；
- 最近一次本机真实额度在 45 秒内复用，再次点击刷新可立即返回，并保留原始来源与更新时间；
- Analytics 改为额度显示后的独立后台补充，不再占用刷新按钮任务或拦截下一次刷新；
- 本机额度源超时由通用 15 秒收紧为 7 秒，失败时更快进入缓存或估算降级。

### 修复

- 额度先保存和通知，Analytics 返回后更新同一条历史记录，避免异步补充产生重复快照。

## 1.8.3 — 2026-07-16

### 修复

- 将稳定按压反馈扩展至主按钮、玻璃按钮、紧凑按钮和菜单工具按钮，并彻底移除这些样式的缩放变换层；
- 移除 Dashboard 与设置页选择变化时作用于整块内容的弹簧和位移动画；
- 移除背景渐变层的额外缩放，避免父级动画继承造成整体放大的视觉效果；
- 保留按钮自身的填充色和透明度变化，使点击状态仍然清晰可见。

## 1.8.2 — 2026-07-16

### 修复

- 移除菜单面板按钮按压时的缩放和弹簧动画，解决 `MenuBarExtra(.window)` 宿主可能将局部缩放表现为整块面板放大的问题；
- 为紧凑操作和工具按钮增加仅作用于按钮自身的颜色、填充和透明度反馈；
- 新增回归测试，确保按下和未按下状态不改变布局缩放。

### 文档

- 补齐功能、数据来源、读取范围、准确性、架构、测试、打包、常见问题和免责声明；
- 新增贡献指南、安全策略及 GitHub Issue/PR 模板；
- 同步构建报告至 1.8.2（Build 25）和 63 项测试。

## 1.8.1 — 2026-07-16

- 完善本机 Codex app-server 额度、Token 日汇总与账户摘要读取；
- 增加本机实时今日 Token、独立 Analytics 合并及紧凑面板里程碑；
- 完善 Universal 2 DMG 打包、登录项、隐私诊断和脱敏测试。
